# Baseline 2026-05-08

End-of-day baseline после доводки движка. Pipeline стабилен, эталонные прогоны зелёные.

## Конфигурация (forgerace.toml)

- **Кодеры:** `aider-devstral` (litellm proxy), `aider-gptoss` (direct nvidia + edit-format=diff)
- **Ревьюеры:** `gemini-api`, `llama`, `qwen-api`, `devstral`, `gpt-oss`, `goose-devstral`
- **Merger:** `gpt-oss` (LLM-based)
- **Лимиты:** `agent_timeout=300`, `progress_timeout=600`, `review_timeout=90`, `max_review_rounds=2`
- **Build:** `pytest -x -q` в worktree + check_command после merge

## Эталонные прогоны (после фикса proxy-stripping)

| Задача | Время | Победитель | Кодеров | Ревьюеров APPROVED | Merge |
|---|---|---|---|---|---|
| TASK-042 `format_bytes` | 3 минуты | aider-gptoss | 1 (solo) | 5/6 | ✅ |
| TASK-043 `truncate_string` | 56 секунд | aider-gptoss | 2 (race) | 5/6 | ✅ |
| TASK-044 `clamp` | 5 минут | aider-devstral | 2 (race с whole-format gptoss) | 5/6 | ✅ |
| TASK-045 `is_blank` | 5 минут | aider-gptoss | 2 (race) | **6/6** (goose APPROVED!) | ✅ |

## Стоимость

**$0.00** на всех прогонах. Всё через nvidia free tier и litellm proxy.

## Тесты

- **103 passed**, 24 skipped (e2e dashboard)
- ruff lint чист
- 5 утилит сделаны агентами: `format_duration`, `format_bytes`, `truncate_string`, `clamp`, `is_blank`

## Ключевые фиксы 2026-05-08

`5f387b3 fix: aider-gptoss больше не зависает + 5 сопутствующих багов`
- proxy-stripping в `_run_agent_text` (3 места) — корпоративный HTTP_PROXY теперь сохраняется
- `forgerace.toml` aider-gptoss.env: убраны пустые `HTTP_PROXY=""` override
- `_multiline_field` парсер для description/integration/acceptance/forbidden/verification
- `cfg.review_timeout=90` per-attempt (retry × 3 в review.py)
- `probe-agents --baseline` — alert при регрессе latency >2×
- stderr=STDOUT в `_run_agent_text` — защита от deadlock на pipe-buffer

`55b637b fix: goose-devstral review запускался без 'run -i'`
- Добавлены `review_args` для goose-devstral. Без них `run_text_agent` запускал голый `goose` → попадал в interactive welcome с telemetry-prompt'ом → review FAILED.
- TASK-041 → done в TASKS.md.

`42f6c83 fix: run_text_agent для CLI пробрасывает acfg.env (OPENAI_API_KEY и т.п.)`
- После `review_args` фикса goose стартовал, но падал с 401 — `proc_env` не включал `acfg.env`. Теперь включает.

## Открытое для следующей сессии

- Чемпионат / pilot (project_championship.md): 7 запланированных фич, 3 кодера верифицированы.
- aider-devstral в race с aider-gptoss diff — кто первым? Текущая статистика: gptoss быстрее на простых утилитах (26с до Applied), devstral медленнее но стабильнее. Race-результат зависит от задачи.
- TASK-040 формат файла: после code fence `` ``` `` в TASKS.md есть лишняя строка `\`\`\`` — может ломать парсер при добавлении новых задач (мелкое).
